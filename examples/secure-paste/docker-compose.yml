services:
  securepaste:
    build: .
    ports:
      - "8000:8000"
    environment:
      - AES_KEY=${AES_KEY:-}
    volumes:
      - data_vol:/app/data
    restart: unless-stopped

volumes:
  data_vol:
