# SecurePaste Environment Configuration
# Copy this file to .env and fill in your values

# 1. Generate a key: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# 2. Paste it below. If left empty, a temporary key will be generated (data lost on restart).
AES_KEY=
